✅ Corrected .gitleaks.toml
title = "TradingBot gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow placeholder/example secret files and known-safe patterns"

paths = [
  '''\.env\.example$''',
  '''docs/.*'''
]

regexes = [
  '''changeme''',
  '''not-a-real-key'''
]